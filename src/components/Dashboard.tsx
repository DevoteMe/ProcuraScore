{/* This file is no longer used directly by routing. */}
      {/* Its functionality has been split into UserLayout.tsx and AdminLayout.tsx */}
      {/* We can keep it for reference temporarily or delete it. */}
      {/* For now, let's clear its content to avoid confusion. */}
      import React from 'react';

      const DeprecatedDashboard: React.FC = () => {
        return (
          <div>
            This component is deprecated. Functionality moved to UserLayout and AdminLayout.
          </div>
        );
      };

      export default DeprecatedDashboard;
