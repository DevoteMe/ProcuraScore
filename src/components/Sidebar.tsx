{/* This file is no longer used directly by routing. */}
      {/* Its functionality has been split into UserSidebar.tsx and AdminSidebar.tsx */}
      {/* We can keep it for reference temporarily or delete it. */}
      {/* For now, let's clear its content to avoid confusion. */}
       import React from 'react';

      const DeprecatedSidebar: React.FC = () => {
        return (
          <div>
            This component is deprecated. Functionality moved to UserSidebar and AdminSidebar.
          </div>
        );
      };

      export default DeprecatedSidebar;
